map $http_upgrade $connection_upgrade {
  default upgrade;
  ''      close;
}

server {
  listen 80;
  listen [::]:80;
  server_name _;
  return 301 https://$host$request_uri;
}

server {
  listen 443 ssl;
  http2 on;
  listen [::]:443 ssl;
  server_name acs.shezanet.net;

  ssl_certificate /etc/nginx/certs/bootstrap/fullchain.pem;
  ssl_certificate_key /etc/nginx/certs/bootstrap/privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;

  add_header Strict-Transport-Security "max-age=31536000" always;

  location = /healthz {
    return 200 'ok';
    add_header Content-Type text/plain;
  }

  location / {
    proxy_pass http://genieacs-cwmp:7547;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_read_timeout 300s;
  }
}

server {
  listen 443 ssl;
  http2 on;
  listen [::]:443 ssl;
  server_name ui.shezanet.net;

  ssl_certificate /etc/nginx/certs/bootstrap/fullchain.pem;
  ssl_certificate_key /etc/nginx/certs/bootstrap/privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;

  location / {
    proxy_pass http://genieacs-ui:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_read_timeout 300s;
  }
}

server {
  listen 443 ssl;
  http2 on;
  listen [::]:443 ssl;
  server_name nbi.shezanet.net;

  ssl_certificate /etc/nginx/certs/bootstrap/fullchain.pem;
  ssl_certificate_key /etc/nginx/certs/bootstrap/privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;

  location / {
    proxy_pass http://genieacs-nbi:7557;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_read_timeout 300s;
  }
}

server {
  listen 443 ssl;
  http2 on;
  listen [::]:443 ssl;
  server_name fs.shezanet.net;

  ssl_certificate /etc/nginx/certs/bootstrap/fullchain.pem;
  ssl_certificate_key /etc/nginx/certs/bootstrap/privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;

  location / {
    proxy_pass http://genieacs-fs:7567;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_read_timeout 300s;
  }
}
